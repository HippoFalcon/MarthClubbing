function setup() {
  createCanvas( windowWidth , windowHeight );
}

function draw() {
  ellipse( mouseX , mouseY , mouseX , mouseY  );
  fill( 9 , 133, 255 );
  noStroke();
  background(255 , 73 , 43 , 40 );
}
