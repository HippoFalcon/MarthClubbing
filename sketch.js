function setup() {
  createCanvas( windowWidth , windowHeight );
}

function draw() {
  ellipse( mouseX , mouseX , mouseY , mouseY  );
  fill( 9 , 133, 25 );
  noStroke();
  background(145 , 73 , 43 , 40 );
}
